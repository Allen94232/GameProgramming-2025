# 新增 Tutorial 和 Level 2 關卡 - 完整操作流程

## ✅ 已完成的程式碼修改

### 1. GameManager.cs
- ✅ `availableLevels` 陣列已更新為：`["Tutorial", "Level 1", "Level 2"]`

### 2. AudioManager.cs
- ✅ 新增 `tutorialBGM` 音樂欄位
- ✅ 新增 `level2BGM` 音樂欄位
- ✅ `PlaySceneBGM()` 方法已新增 Tutorial 和 Level 2 的音樂切換邏輯

### 3. MainMenuController.cs
- ✅ 新增 `_tutorialButton` 和 `_level2Button` 變數
- ✅ 新增按鈕點擊事件處理
- ✅ 新增 hover 音效

### 4. MainMenu.uxml
- ✅ 在選關面板新增三個按鈕：Tutorial、Level 1、Level 2

---

## 🎯 Unity Editor 中需要執行的操作

### 步驟 1：創建場景文件
1. **創建 Tutorial 場景**
   - File → New Scene
   - 命名為 `Tutorial`
   - 保存到 `Assets/Scenes/` 資料夾
   - 可以複製 Level 1 場景作為基礎，然後修改

2. **創建 Level 2 場景**
   - File → New Scene
   - 命名為 `Level 2`
   - 保存到 `Assets/Scenes/` 資料夾

### 步驟 2：將場景加入 Build Settings
1. 打開 Build Settings (File → Build Settings 或 Ctrl+Shift+B)
2. 點擊 "Add Open Scenes" 或直接拖拽場景文件
3. 確保場景順序如下：
   ```
   0: MainMenuScene
   1: Tutorial
   2: Level 1
   3: Level 2
   ```
4. 點擊 "Save" 或關閉視窗

### 步驟 3：設定 AudioManager BGM
1. 在 Hierarchy 中找到 `AudioManager` GameObject（通常在 MainMenuScene 中）
2. 在 Inspector 視窗中找到 AudioManager 組件
3. 在 **Background Music** 區塊設定：
   - **Tutorial BGM**: 拖入教學關卡的音樂檔案
   - **Level 1 BGM**: 拖入關卡 1 的音樂檔案（如果還沒設定）
   - **Level 2 BGM**: 拖入關卡 2 的音樂檔案

   > 💡 **提示**：如果目前沒有不同的音樂，可以：
   > - 暫時都使用同一個音樂檔案
   > - 或留空（系統會自動使用 Level 1 BGM 作為 fallback）

### 步驟 4：設定 LeaderboardManager 排行榜
1. 在 Hierarchy 中找到 `LeaderboardManager` GameObject
2. 在 Inspector 視窗中找到 LeaderboardManager 組件
3. 在 **Multi-Level Leaderboard Configuration** 區塊：
   - 將 `Leaderboard Configs` 的 Size 改為 **3**
   - 設定三個關卡的排行榜：

   ```
   Element 0:
   - Level Name: Tutorial
   - Leaderboard Id: [你的 Tutorial 排行榜 ID]

   Element 1:
   - Level Name: Level 1
   - Leaderboard Id: [你的 Level 1 排行榜 ID]

   Element 2:
   - Level Name: Level 2
   - Leaderboard Id: [你的 Level 2 排行榜 ID]
   ```

   > 💡 **如何獲取 Leaderboard ID**：
   > 1. 登入 LootLocker Dashboard (https://console.lootlocker.com/)
   > 2. 進入你的遊戲專案
   > 3. 左側選單 → Leaderboards
   > 4. 為每個關卡創建新的 Leaderboard（如果還沒有）
   > 5. 複製每個 Leaderboard 的 ID

### 步驟 5：設計關卡內容

#### Tutorial 場景設計建議：
- 簡化版的地圖（較小）
- 加入教學提示 UI
- 減少障礙物和 NPC 數量
- 設定較容易達成的目標
- 可以加入分步驟的教學流程

#### Level 2 場景設計建議：
- 比 Level 1 更大或更複雜的地圖
- 增加障礙物數量
- 增加 NPC（行人、車輛）數量
- 更具挑戰性的目標設定
- 可以加入新的遊戲元素

### 步驟 6：測試流程
1. **測試主選單**
   - 運行 MainMenuScene
   - 點擊 "START GAME" 按鈕
   - 確認能看到三個關卡按鈕：Tutorial、Level 1、Level 2
   - 測試每個按鈕是否能正確載入對應場景

2. **測試音樂切換**
   - 進入每個關卡
   - 確認 BGM 正確播放
   - 測試場景切換時音樂是否正確重新開始

3. **測試排行榜**
   - 點擊主選單的 "LEADERBOARD" 按鈕
   - 使用 ← → 按鈕切換關卡
   - 確認能看到三個關卡：Tutorial、Level 1、Level 2
   - 測試每個關卡的排行榜是否正確顯示

4. **測試關卡完成**
   - 進入每個關卡並完成遊戲
   - 確認分數正確上傳到對應的排行榜
   - 確認排行榜顯示正確的分數

---

## 📋 快速檢查清單

- [ ] 創建 Tutorial 場景並保存
- [ ] 創建 Level 2 場景並保存
- [ ] Build Settings 中加入兩個新場景
- [ ] AudioManager 設定 Tutorial BGM
- [ ] AudioManager 設定 Level 2 BGM
- [ ] LeaderboardManager 新增 Tutorial 排行榜設定
- [ ] LeaderboardManager 新增 Level 2 排行榜設定
- [ ] Tutorial 場景設計完成
- [ ] Level 2 場景設計完成
- [ ] 測試主選單選關功能
- [ ] 測試音樂切換功能
- [ ] 測試排行榜切換功能
- [ ] 測試關卡完成和分數上傳

---

## 🔧 常見問題排除

### Q1: 點擊按鈕後場景無法載入
**A:** 檢查以下項目：
1. 場景是否已加入 Build Settings
2. 場景名稱是否完全正確（大小寫敏感）
3. Console 是否有錯誤訊息

### Q2: 音樂沒有播放
**A:** 檢查以下項目：
1. AudioManager 中是否已指派音樂檔案
2. 音樂檔案是否正確匯入（Import Settings）
3. AudioManager GameObject 是否存在於場景中
4. 音量設定是否為 0

### Q3: 排行榜顯示錯誤
**A:** 檢查以下項目：
1. LeaderboardManager 中的 Level Name 是否和場景名稱完全一致
2. Leaderboard ID 是否正確
3. LootLocker 連線是否正常
4. Console 是否有 API 錯誤訊息

### Q4: 選關面板按鈕沒有反應
**A:** 檢查以下項目：
1. MainMenu.uxml 中按鈕的 name 屬性是否正確
2. MainMenuController.cs 中是否正確綁定按鈕
3. 按鈕是否被其他 UI 元素遮擋

---

## 🎉 完成後的功能

完成所有步驟後，你的遊戲將擁有：
- ✨ 主選單顯示三個關卡選項
- 🎵 每個關卡可以有獨立的背景音樂
- 🏆 每個關卡有獨立的排行榜系統
- 📊 排行榜可以切換查看不同關卡的成績
- 🎮 完整的關卡選擇和遊玩流程

---

## 📝 額外建議

### 關卡設計階段性開發：
1. **階段 1**：先完成 Tutorial 關卡
   - 設計簡單的教學流程
   - 確保新手能理解遊戲機制

2. **階段 2**：複製並修改 Level 1 創建 Level 2
   - 增加難度
   - 測試平衡性

3. **階段 3**：優化和調整
   - 根據測試反饋調整
   - 確保三個關卡難度遞增合理

### 未來擴展：
- 如需新增更多關卡，重複以上步驟
- 記得同步更新：GameManager、AudioManager、LeaderboardManager
- 考慮實作關卡解鎖系統（需完成前一關才能進入下一關）
